{
  "name": "aipaint",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic, agent-operable pixel painting on GitHub Pages",
  "scripts": {
    "test": "node --test tests/core.test.js",
    "start": "python3 -m http.server 8000 --bind 127.0.0.1",
    "test:browser": "python3 tests/browser_test.py",
    "render": "python3 scripts/render.py"
  },
  "engines": { "node": ">=22" }
}
