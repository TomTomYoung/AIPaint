# 実装状況

更新日：2026-09-08。実装版：0.1.0。

## 実装済み

ドット描画エンジン、13種類のコマンド、レイヤー構成・ロック・表示・不透明度、原稿スナップショット、一括操作、revision競合、セッション内再送排除、Undo/Redo、日本語UI、ドラッグプレビュー、PNG/原稿出力、原稿読込、ブラウザ内自動保存、JSON操作、検証付きサンプルジョブ、Playwright自動描画スクリプトを実装しています。

GitHub ActionsのCI定義と、仕様・進捗・引き継ぎ文書を追加しています。CIは読み取り権限でテストし、成果物をActions artifactへ出す構成です。これは素材の別リポジトリ格納機能ではありません。

## ローカル検証

Node.js v22.16.0でコアテスト27件が成功しました。描画、透過、レイヤー、ロック、競合、原子性、再送、Undo/Redo、原稿往復、入力拒否、上限、ジョブの再現性を含みます。

通常のHTTPブラウザ統合試験10件は、この作業環境のChromiumがページ遷移を管理ポリシーで拒否したため実行成立していません。アプリの試験失敗とは別の環境ブロックですが、成功扱いにはしていません。

代わりにネットワークを使用せず、同じHTML/CSS/JavaScriptをブラウザ内へ渡すオフライン確認を実施しました。画面起動、ポインター描画、Undo/Redo、サンプルジョブ、PNG生成、転送ハッシュ一致、デスクトップ/モバイル画面の取得に成功しています。この確認では原点のないページ向けにstorage・fetch・WebCryptoの一部をテスト用に置き換えているため、CSP、HTTP配信、GitHub Pages、永続ストレージの受入試験を代替しません。

## GitHub Actionsでの検証

コードコミット：`8d72e54f6c3a794084167faaa7cefdd936597949`。mainへ反映済みです。

[CI実行 #2](https://github.com/TomTomYoung/AIPaint/actions/runs/34224831153)はsuccessで完了しました。Node.jsのコアテスト27件、HTTP配信したChromiumでのブラウザ統合テスト10件、自動描画CLIの実行、成果物artifactのアップロードがすべて成功しています。

ブラウザ試験では、Pages相当のサブパス起動、ポインター描画、Undo/Redo、不正な一括命令の拒否、レイヤー付き原稿の往復、PNG復号後の画素一致、revision競合、未接続Publisherの明示、レイヤー操作、既存自動保存の保護、失敗ジョブの原稿保護、デスクトップ/モバイル画面を確認しています。

自動描画CLIはサンプルジョブからPNG、プレビュー、レイヤーPNG、原稿JSON、コマンド、ハッシュ付きmanifestを出力しました。成果物はActions artifactであり、別リポジトリに保存したものではありません。

最初のCI実行では、ブラウザ試験の待機処理3件がCSPによるeval拒否で失敗しました。アプリのCSPは変更せず、テストとCLIの待機をPython側の限定された状態ポーリングへ修正しています。手元でもCSPを維持した再現試験で、旧待機の失敗と修正後の成功を確認しました。上記のCI実行 #2が修正後の結果です。

## 公開状態

確認時点のGitHub APIでは`has_pages: false`です。Pagesの公開設定は今回変更していません。公開する場合はSettings → Pages → Deploy from a branch → main → /(root)を指定し、配信後に実際のURLを開いて確認してください。CI内のサブパス試験成功と、GitHub Pagesへの公開成功は区別します。

## 次の到達点

次はPages公開確認を行います。その後、素材リポジトリBを指定し、固定アプリ版の取得、ジョブ受付、描画と公開の権限分離、PR保存、読み戻し検証を実装します。PNGを作れたことだけで当初目的の全体完成とはしません。
